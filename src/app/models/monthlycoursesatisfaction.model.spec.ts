import { Monthlycoursesatisfaction } from './monthlycoursesatisfaction.model';

describe('Monthlycoursesatisfaction', () => {
  it('should create an instance', () => {
    expect(new Monthlycoursesatisfaction()).toBeTruthy();
  });
});
