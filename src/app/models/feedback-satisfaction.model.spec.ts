import { FeedbackSatisfaction } from './feedback-satisfaction.model';

describe('FeedbackSatisfaction', () => {
  it('should create an instance', () => {
    expect(new FeedbackSatisfaction()).toBeTruthy();
  });
});
