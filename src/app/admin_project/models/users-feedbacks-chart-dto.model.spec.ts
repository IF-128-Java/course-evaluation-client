import { UsersFeedbacksChartDto } from './users-feedbacks-chart-dto.model';

describe('UsersFeedbacksChartDto', () => {
  it('should create an instance', () => {
    expect(new UsersFeedbacksChartDto()).toBeTruthy();
  });
});
