import { ChatsMessagesDto } from './chats-messages-dto.model';

describe('ChatsMessagesDto', () => {
  it('should create an instance', () => {
    expect(new ChatsMessagesDto()).toBeTruthy();
  });
});
